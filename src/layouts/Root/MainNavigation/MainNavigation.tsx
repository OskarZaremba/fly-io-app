import type { FC } from 'react';

export const MainNavigation: FC = () => {
	return (
		<header className="sticky top-0 w-full bg-white shadow">
			<nav className="px-8 py-4">Hello 👋, I am a MainNavigation component</nav>
		</header>
	);
};
